Before installing anything

sudo apt update
sudo apt install openjdk-17-jdk -y
java --version
--> as its ubuntu, as its a debian packager, you need to install few pluggins in pkg.jenkins.io , we need those packages to install jenkin servers inside ubuntu instances
-->Ubuntu trusts Jenkins packages downloaded from pkg.jenkins.io
-->Run the following commands to add the Jenkins official repository:

curl -fsSL https://pkg.jenkins.io/debian/jenkins.io-2023.key | sudo tee \
  /usr/share/keyrings/jenkins-keyring.asc > /dev/null

-->once above command runt, it will add a key 
Then add the repository:

echo deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] \
  https://pkg.jenkins.io/debian binary/ | sudo tee \
  /etc/apt/sources.list.d/jenkins.list > /dev/null

sudo apt-get update
  sudo apt-get install fontconfig openjdk-21-jre
  sudo apt-get install jenkins

-->Start and enable Jenkins service
sudo systemctl start jenkins
-->Enable it to start on boot:
sudo systemctl enable jenkins
-->status check
sudo systemctl status jenkins
-->take ip :8080 -it wont access as port is not setup in instance
-->to do that -instance--security--security groups-edit-inbound rules-edit-custom tcp-define port 8080-set rules
-->once done to access jenkins pwd , you need to copy the path showing and paste it using below command
sudo cat /var/lib/jenkins/secrets/initialAdminPassword

Useful links: https://pkg.jenkins.io/debian/
https://chatgpt.com/g/g-p-68d80a055f948191baa7006a7528cdbc-keer/c/690ef201-087c-8321-bd60-1aab7cff4ed4
